trigger:
- main

pool:
  vmImage: 'windows-latest'

jobs:
- job: RunPowerShellScript
  steps:
  - task: PowerShell@2
    inputs:
      targetType: 'filePath'
      filePath: 'path/to/myscript.ps1'
Get-ChildItem -Path .\MyProject -Recurse -File | ForEach-Object { $_.FullName }
